commands:
  01_install_chrome:
    command: |
      # Add Chrome repository
      curl https://intoli.com/install-google-chrome.sh | bash
      sudo mv /usr/bin/google-chrome-stable /usr/bin/google-chrome -y
      google-chrome --version && which google-chrome